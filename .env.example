# N8N Workflow Backup Tool - Environment Variables
# Copy this file to .env and fill with your credentials

# SOURCE N8N Instance (for download/backup)
SOURCE_N8N_URL=https://your-source-n8n-instance.com/
SOURCE_N8N_API_KEY=your-source-api-key-here
SOURCE_N8N_TAG=production

# TARGET N8N Instance (for upload/restore)
# Use the interactive menu option "Configure Target N8N Instance" to set these
# Or manually configure:
TARGET_N8N_URL=https://your-target-n8n-instance.com/
TARGET_N8N_API_KEY=your-target-api-key-here
TARGET_N8N_TAG=production

# Legacy variables (fallback to SOURCE if not using multi-instance)
N8N_URL=https://your-n8n-instance.com/
N8N_API_KEY=your-api-key-here

# Authentication Method 2: Username/Password (alternative - not recommended)
# N8N_USERNAME=your-username
# N8N_PASSWORD=your-password

# Optional: Filter workflows by tag (fallback)
N8N_TAG=production
# Note: SOURCE_N8N_TAG takes precedence for downloads when SOURCE_N8N_URL is set

# Optional: Log level (debug, info, warn, error)
# LOG_LEVEL=info

# Upload Configuration
# N8N_INPUT_DIR=./n8n-workflows-{timestamp}

# Outline Configuration
OUTLINE_URL=https://your-outline-instance.com
OUTLINE_API_TOKEN=your-outline-api-token-here
